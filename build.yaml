# ---
# include:
#   - board: eyelash_sofle_right
#     shield: nice_view
#   - board: eyelash_sofle_left
#     shield: nice_view
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
#     artifact-name: eyelash_sofle_studio_left
#   - board: nice_nano_v2
#     shield: settings_reset
#   - name: Upgrade West
#     run: |
#       pip3 install -U west
jobs:
  build:
    strategy:
      matrix:
        include:
          - board: eyelash_sofle_left
            artifact-name: eyelash_sofle_left
          - board: eyelash_sofle_right
            artifact-name: eyelash_sofle_right
          - board: eyelash_sofle_left
            snippet: studio-rpc-usb-uart
            cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
            artifact-name: eyelash_sofle_studio_left
          - board: eyelash_sofle_left
            shield: settings_reset
            artifact-name: eyelash_sofle_settings_reset

    steps:
      # Put the West upgrade step at the beginning of your steps block
      - name: Upgrade West
        run: |
          pip3 install -U west

      # Your remaining checkout, initialization, and west build steps follow below...
